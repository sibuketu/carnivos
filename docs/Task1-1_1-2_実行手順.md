# Task 1-1 / 1-2: Lint と型チェック

やることだけ。

---

## 1. Lint と型チェック（コピペ一発）

次をまとめてコピペして実行。**PowerShell では `cd /d` は使わない**（cmd 用）。`cd "パス"` のみ。すでにプロジェクトフォルダにいるなら 1 行目は不要。PowerShell で npx が署名エラーになる場合は、型チェックだけ `npm run typecheck` または `node node_modules/typescript/bin/tsc --noEmit` で動く。

```powershell
cd "C:\Users\susam\Downloads\primal-logic-docs\docs\primal-logic-app\primal-logic-web"
npm run lint -- --fix
npm run typecheck
```

完了の目安: 両方エラー 0 件。

---

## 2. アプリ起動（コピペ一発）

```powershell
cd "C:\Users\susam\Downloads\primal-logic-docs\docs\primal-logic-app\primal-logic-web"
npm run dev
```

ブラウザで http://localhost:5173 を開く。

---

エクスプローラーから **run-code-check.bat** をダブルクリックでも Lint+型チェックできる（cmd で動くので npx も通る）。
